import { StoreProvider, Dependency, Injectable } from '../types';
import { DiError } from './error';
export declare class MissingProviderError extends DiError {
    constructor(provider: StoreProvider | Injectable, dep?: Dependency);
}
