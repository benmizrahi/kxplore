import { StoreProvider, Injectable } from '../types';
/**
 * Get id of provider or injectable
 *
 * @protected
 * @param {(StoreProvider | Injectable)} injectable
 *
 * @returns {string}
 */
export declare function id(injectable: StoreProvider | Injectable): string;
export declare class DiError extends Error {
    name: string;
    message: string;
    constructor(message: string);
}
