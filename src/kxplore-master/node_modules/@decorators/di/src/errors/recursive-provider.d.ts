import { StoreProvider } from '../types';
import { DiError } from './error';
export declare class RecursiveProviderError extends DiError {
    constructor(requesters: StoreProvider[], depProvider: StoreProvider);
}
